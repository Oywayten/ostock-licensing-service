server:
  port: 8071

spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config
        git:
          uri: https://github.com/Oywayten/config.git
          search-paths: licensing-service
          default-label: master
        vault:
          kv-version: 2
          host: vaultserver

